# Product films v1 (archived)

Scroll-driven Three.js product films (ProductStage + F1Film + IronManFilm).

Kept for reference and reachable at:

- `/archive/f1` — AMR23 v1
- `/archive/iron-man` — Mark 85 v1
- `/f1-w13`, `/iron-man-body`, `/watch` — still use this stack

**Primary product URLs** `/f1` and `/iron-man` are **v3 hybrid** (Garage3D + GSAP sections).  
See [`AGENTS.md`](../../../AGENTS.md) and [`docs/REBUILD_F1_IRONMAN.md`](../../../docs/REBUILD_F1_IRONMAN.md).

Do not extend ProductStage for contest polish — work in `src/lib/garageStage/` instead.
